# 代理访问密码
API_KEYS=

# 认证配置 (填入 auth_single 内容，多账号依次增加 AUTH_JSON_2...)
# AUTH_JSON_1=

# 单账号使用多少次后切换 (建议≤50)
SWITCH_ON_USES=40

# 遇到以下状态码立即切换账号
IMMEDIATE_SWITCH_STATUS_CODES=401,403,429

# 起始账号索引 (null为默认)
INITIAL_AUTH_INDEX=null

# 流式模式 (real=真流式, fake=假流式)
STREAMING_MODE=real